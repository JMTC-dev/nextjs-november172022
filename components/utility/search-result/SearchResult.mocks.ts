import { ISearchResult } from './SearchResult';

const base: ISearchResult = {
  url: 'https://www.google.com',
  title: 'Google',
  text: 'One of the largest search engines currently available.',
};

export const mockSearchResultProps = {
  base,
};
